version: 2

seeds:
  - name: ga4_source_categories
    description: "Google's mapping between source and source_category. (Downloaded from https://support.google.com/analytics/answer/9756891?hl=en)"
    columns:
      - name: source
        tests:
          - not_null

      - name: source_category
        tests:
          - not_null
